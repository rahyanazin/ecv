<h1><%= @subject.title %></h1>
<div>
  <%= @subject.description %>
</div>
<br>
<div>
  Visível: <%= @subject.visible %>
</div>
<br>
<div>
  Novo: <%= @subject.new %>
</div>
<br>
<%= link_to 'Editar', edit_subject_path(@subject) %> | 

<%= link_to 'Excluir', subject_path(@subject), method: :delete, data: { confirm: "Are you sure?" } %>